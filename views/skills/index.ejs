<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Skills</title>
</head>
<body>
  <h1>Skills</h1>
  <ul>
    <% skills.forEach(skill => { %>
      <li>
			<a href="/skills/<%= skill._id %>"><%= skill.text %></a> 
			<%= skill.done ? 'mastered' : ' not mastered' %>
			</li>
    <% }) %>
  </ul>
  <a href="/skills/new">Add Skill</a><br>
  <a href="/">Back</a>
</body>
</html>